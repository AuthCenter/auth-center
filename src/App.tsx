import React, { FC } from 'react';
import Animation from './views/Login/Animation';
const App: FC<any> = (props) => {
	return (
		<div className='app'>
			<Animation />
		</div>
	);
};

export { App };
